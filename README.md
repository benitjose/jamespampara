# jamespampara

## Database
This app now uses PostgreSQL instead of SQLite for persistent storage.

Set a database URL before running the app:

```bash
export DATABASE_URL=postgresql://neondb_owner:npg_Xlhntx02vTNw@ep-lucky-heart-axdcmydb.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require
```

Make sure the PostgreSQL server is running and the database exists.
